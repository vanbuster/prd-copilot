EVALUATE_PROMPT = """你是一位严格的 PRD 质量审查专家。请对以下 PRD 进行质量评分。

## PRD 内容
{prd_content}

---

请从三个维度评分（每项 0-100 分），并给出具体改进建议。

## 评分标准

### 1. 完整性（0-100）
检查以下 11 个模块是否齐全，内容是否充实：
- **必检模块**（缺少扣 15 分）：
  - 产品概述（名称定位、背景问题、核心价值、SMART 目标）
  - 目标用户（用户画像、使用场景、用户旅程）
  - 用户故事与需求分析（编号 US1/US2...、As a/I want/So that 格式、验收标准含正常+边界+异常路径）
  - 功能需求（编号 F1/F2...、触发条件、预期行为、MoSCoW 优先级、业务规则）
  - 非功能需求（性能、可用性、安全、兼容性、可扩展性）
  - 信息架构与交互流程（页面结构树、2+ 个含异常分支的交互流程、状态说明）
  - 评估指标（1 个北极星指标、KPI 表格、5+ 个埋点事件）
- **新增模块**（缺少扣 10 分）：
  - 不做什么（至少 3 项排除功能及原因）
  - 假设与约束（至少 3 个假设含验证方式和备选方案）
  - 风险评估（至少 3 个风险含缓解措施）
  - 里程碑与发布计划（至少 2 个阶段含交付标准）
- 子项缺失扣 3-5 分

### 2. 量化度（0-100）
- 非功能需求是否全部包含量化数字（如"≤ 2s"而非"快速"）且标注了影响的技术决策
- 评估指标是否有具体目标值、当前基线和统计周期
- 产品目标是否符合 SMART 原则（具体、可衡量、有截止时间）
- 用户故事验收标准是否可验证（能通过测试用例验证）
- 发现一个空洞表述（如"优化体验""提升性能""简单易用"）扣 10 分
- 性能指标缺少测量条件扣 5 分
- 安全要求缺少 OWASP 分类扣 3 分

### 3. 可执行性（0-100）
- 功能需求是否有明确的触发条件和预期行为（成功和失败情况）
- 验收标准是否覆盖正常路径 + 边界条件 + 异常路径
- 交互流程是否每个步骤都有异常分支处理
- 信息架构是否清晰到可以开始 UI 设计
- 业务规则是否有明确描述（权限判断、状态流转、计算公式）
- 需求优先级矩阵是否包含用户价值、实现复杂度、依赖关系
- "不做什么"模块是否有明确的排除原因
- 假设是否有不成立时的备选方案
- 风险是否有具体的缓解措施和负责人

## 输出格式

严格输出以下 JSON，不要输出其他内容：

```json
{{
  "completeness": {{
    "score": 0,
    "details": "具体扣分项说明，列出缺失或不足的模块"
  }},
  "quantification": {{
    "score": 0,
    "details": "具体扣分项说明，列出未量化的指标"
  }},
  "executability": {{
    "score": 0,
    "details": "具体扣分项说明，列出不可执行的部分"
  }},
  "total_score": 0,
  "improvements": [
    "具体改进建议1（标注优先级 P0/P1）",
    "具体改进建议2",
    "具体改进建议3"
  ],
  "highlights": [
    "做得好的地方1",
    "做得好的地方2"
  ]
}}
```
"""
