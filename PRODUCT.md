# 产品说明

## 我如何定义「一份好的 PRD 大纲」

一份好的 PRD 大纲应该是**可执行、有结构、能量化**的。它不只是产品经理的思考记录，更是开发团队的行动指南。具体来说：每个需求都有明确的验收标准（而非"优化体验"这种模糊表述），非功能需求包含量化指标（如"< 2s 响应时间"），用户故事遵循标准格式，优先级用 MoSCoW 方法标注。参考了 OpenAI PM Lead Miqdad Jaffer 的 AI PRD 模板和 ChatPRD 的模板体系，我设计了「7+1」模块结构——7 个核心模块覆盖产品概述到评估指标的完整链路，第 8 个 AI 专项模块在检测到 AI 产品时自动激活，包含模型能力边界、Prompt 设计策略和降级方案。

## 我做了哪些产品决策，为什么

**决策一：多轮对话而非一次性生成。** 参考 MetaGPT 的 SOP 思想，分阶段流程比单次生成质量更高。澄清环节让用户补全关键信息（目标用户、核心场景、功能边界），生成的 PRD 更精准。

**决策二：AI 产品自动检测。** AI 产品的 PRD 和传统产品有本质区别（参考 woshipm.com 的分析），需要额外的模型能力边界和 Prompt 设计章节。通过关键词检测自动判断，免去用户手动选择。

**决策三：逐节精修模式。** 先生成完整大纲，再让用户选择某个模块展开。这比一次生成所有细节更高效，也避免了信息过载。

## v1 发布后已补齐（原「如果再给我一周」清单）

初版写下的 5 项优化中，3 项已在后续迭代中落地：

1. ✅ **竞品分析模块**：LLM function calling 接入 web 搜索，自动生成竞品对比（`prompts/competitive.py` + `utils/search.py`，commit `50d8353`）
2. ✅ **多模板支持**：6 个行业模板（通用 / SaaS / 移动端 / AI 应用 / 电商 / B2B），差异化关注点注入生成提示词（`templates/prd_template.py`，commit `50d8353`）
3. ✅ **PRD 质量评分**：全模块自动评分并给出改进建议（`prompts/evaluate.py`，commit `50d8353` 引入、`3aa8524` 扩展至覆盖 11+1 模块）

仍未做（项目已归档，不再迭代）：

4. **协作审查**：支持多人对 PRD 进行评论和批注，模拟真实的产品评审流程
5. **原型生成**：基于 PRD 的信息架构自动生成低保真线框图
