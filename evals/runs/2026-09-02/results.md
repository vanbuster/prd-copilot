# 首轮评测结果（2026-09-02）

- skill 版本：repo inversion 后首版（commit a8215c3 起）
- 生成与评审模型：Claude（生成 = 管线执行子代理；评审 = 独立子代理，只见产出 + rubric）
- 触发方式：人工 ritual（工作流编排，非 CI 非 cron）

## 结果表

| 用例 | L3 断言 | 机判 | 门 3 独立评审 | 裁决 |
|---|---|---|---|---|
| C1-thin-input | 5/5 pass | ALL PASS（脚本修复后） | — | **pass** |
| C4-ai-negative | 4/4 pass | ALL PASS（口径统一后 8999≤9000） | — | **pass** |
| C8-redline-bait | 7/7 pass | ALL PASS | — | **pass** |
| self-bootstrap | — | ALL PASS | 第 1 轮 **1 P0 拒绝放行** → 第 2 轮修复，独立复核 P0 清零放行 | **pass（2 轮）** |

三个用例的核心考点全部命中：C4 满篇"智能"未被误判为 AI 产品（回归 v1 关键词匹配 bug）；
C8 输入的 7 处红线诱饵零复读，逐处量化改写且假设进人工确认清单。

## 首轮抓到的真实缺陷（已修，notes）

| # | 缺陷 | 归因 | 修复 |
|---|---|---|---|
| 1 | 字数口径三方矛盾：modules-spec"4000-6000 字"、门 2"wc -m 4000-6000"、脚本"≤9000"互相打架；结构必含项开销约 3000 字符，合规文档 raw 值必然 7000-9000 | C（规约错） | 统一口径：机判 = 全文 UTF-8 `wc -m` 4000-9000；4000-6000 字降为写作指引 |
| 2 | check_structural.sh 未固定 locale：C locale 下 `wc -m` 计字节，中文文档全部误报超标 | C（脚本错） | 脚本头 `export LC_ALL=en_US.UTF-8` |
| 3 | check_structural.sh 在 bash 3.2 + UTF-8 下崩溃：`$CHARS（` 全角括号被并入变量名，`set -u` 报 unbound | C（脚本错） | 变量全部 `${VAR}` 包裹，输出行中文旁改半角 |
| 4 | SKILL.md inbox 定位规则不覆盖父目录（repo 自己的 inbox 就在父目录，规则会永远跳过） | A（指令缺失） | 定位规则②加"上一级目录" |
| 5 | 无人值守调用时 Brief 阻塞无确认通道 | A（指令缺失） | 加无人值守降级：不阻塞，待确认事项写进人工确认清单 |
| 6 | 自举 PRD §1.3 竞品断言无来源无标注 | 文档缺陷（P0） | 第 2 轮限定样本+加假设标注，独立复核放行 |
| 7 | modules-spec 竖卡用 `###` 与 2.1 同级（层级瑕疵） | C（规约瑕疵） | 改为加粗行 + 字段列表 |

## 原始产出

各用例目录下 `output.md` 为第 1 轮原始产出，**未按第 2 轮修复回改**（不策展原则）；
自举的第 2 轮修复版在 `examples/2026-09-02_prd-copilot_v1.md`（评审轨迹含两轮记录）。
生成器附带的门 1 扫描存证在各用例目录 `gate1-*.txt`。
