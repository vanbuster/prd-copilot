CLARIFY_PROMPT = """分析以下用户的产品需求描述，识别其中缺失或模糊的关键信息。

用户描述：
{user_input}

请生成 3-5 个关键问题来补全信息。每个问题必须提供 3-4 个选项供用户选择。

问题维度参考（按优先级，跳过已经清晰的信息）：
1. **目标用户**：产品面向谁？
2. **核心场景**：用户在什么情况下使用？
3. **功能边界**：MVP 包含哪些核心功能？
4. **差异化**：和现有产品的区别是什么？
5. **成功标准**：如何衡量产品是否成功？

严格要求：
- 每个问题简洁明确
- 每个问题必须给出 3-4 个选项，选项要具体、有区分度
- 选项要基于用户描述合理推断
- 问题之间不重复

**select_type 判断方法**：逐个审视每个问题的选项，问自己"用户能同时选 A 和 B 吗？"
- 如果选了 A 还能选 B → `"select_type": "multi"`
- 如果选了 A 就排除了 B → `"select_type": "single"`

举例：
- "面向哪类用户"：能同时选大学生+职场人 → multi
- "首发平台"：Web 和 Mobile 是互斥的（只能先做一个） → single
- "MVP 核心功能"：登录+支付可以同时要 → multi
- "商业模式"：免费增值和纯付费互斥 → single
- "核心使用场景"：能同时有多个场景 → multi

以 JSON 数组输出：
[
  {{"question": "面向哪类用户？（可多选）", "options": ["大学生", "职场白领", "企业B端"], "select_type": "multi"}},
  {{"question": "产品的首发平台？", "options": ["Web网页端", "移动端App", "小程序"], "select_type": "single"}}
]

仅输出 JSON 数组，不要输出其他内容。
"""
