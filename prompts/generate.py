GENERATE_PROMPT = """基于以下信息，生成一份详尽、可执行的 PRD（产品需求文档）。

## 用户原始需求
{user_input}

## 澄清问答
{clarify_qa}

## 是否为 AI 产品
{is_ai_product}

---

请严格按照以下模块结构生成 PRD。每个模块都有详细的输出要求，你必须逐条满足。

## 1. 产品概述（300-500 字）

### 1.1 产品名称与定位
- 如果用户未提供产品名，根据功能起一个简洁的名称
- 用一句话定义产品定位（"XX 是一款面向 [目标用户] 的 [产品类型]，核心解决 [什么问题]"）

### 1.2 背景与问题
- 描述当前市场/用户面临的具体问题（2-3 个痛点，每个痛点用 1-2 句话说明）
- 说明为什么现在做这件事（市场时机、技术条件、用户需求变化等）

### 1.3 核心价值主张
- 用一句话概括核心价值
- 列出 3 个核心价值点，每个价值点包含：价值描述 + 对用户的意义

### 1.4 产品目标
- 短期目标（3 个月内）
- 中期目标（6 个月内）
- 用 SMART 原则描述（具体、可衡量、可达成、相关、有时限）

## 2. 目标用户（200-400 字）

### 2.1 用户画像
给出 2-3 个典型用户画像，每个画像包含：
| 字段 | 内容 |
|------|------|
| 姓名 | 虚构但真实的名字 |
| 年龄/职业 | 具体年龄段和职业 |
| 核心痛点 | 1-2 个最迫切的问题 |
| 使用动机 | 为什么会使用这个产品 |
| 使用频率预估 | 每天/每周/每月几次 |
| 技术熟练度 | 新手/一般/熟练 |

### 2.2 使用场景
- 列出 3-5 个典型使用场景，每个场景包含：触发条件 + 用户行为 + 预期结果
- 标注主场景和次场景

## 3. 用户故事与需求分析（400-600 字）

### 3.1 核心用户故事
列出 6-10 个用户故事，严格遵循以下格式：

**P0（必须有）：**
- As a [具体角色], I want [具体操作/功能], So that [具体价值/目的]
  - 验收标准：- [ ] 具体可验证的条件

**P1（应该有）：**
- （同上格式）

**P2（可以有）：**
- （同上格式）

### 3.2 需求优先级矩阵
| 需求 | 用户价值 | 实现复杂度 | 优先级 |
|------|---------|-----------|--------|
| 需求1 | 高/中/低 | 高/中/低 | P0/P1/P2 |

## 4. 功能需求（500-800 字）

按功能模块分组（3-5 个模块），每个模块包含：

### 4.X [模块名称]

**模块目标**：一句话说明这个模块解决什么问题

| 功能点 | 描述 | MoSCoW | 验收标准 |
|--------|------|--------|---------|
| 功能1 | 具体描述做什么 | Must/Should/Could | - [ ] 标准1 - [ ] 标准2 |
| 功能2 | 具体描述做什么 | Must/Should/Could | - [ ] 标准1 - [ ] 标准2 |

**边界说明**：
- 这个模块包含：...
- 这个模块不包含：...

## 5. 非功能需求（200-400 字）

每个维度必须包含量化指标：

### 5.1 性能要求
| 指标 | 目标值 | 测量条件 |
|------|--------|---------|
| 页面加载时间 | ≤ X 秒 | 首屏加载，3G网络 |
| API 响应时间 | ≤ X ms | 95% 请求，并发X用户 |
| 并发支持 | X 用户同时在线 | 峰值时段 |

### 5.2 可用性要求
| 指标 | 目标值 |
|------|--------|
| 系统可用性 | ≥ X% |
| 数据备份频率 | 每 X 小时 |
| 故障恢复时间 | ≤ X 分钟 |

### 5.3 安全要求
- 列出 3-5 条具体安全要求（认证方式、数据加密、权限控制等）

### 5.4 兼容性要求
- 支持的浏览器/设备/系统版本

## 6. 信息架构与交互流程（300-500 字）

### 6.1 页面/模块结构
用缩进列表展示层级结构：
```
产品首页
├── 模块A
│   ├── 子页面A1
│   └── 子页面A2
├── 模块B
│   ├── 子页面B1
│   └── 子页面B2
└── 设置中心
    ├── 个人设置
    └── 系统设置
```

### 6.2 核心交互流程
用编号步骤描述 2-3 个核心流程：
**流程1：[流程名称]**
1. 用户进入 [页面] → 看到 [什么]
2. 用户点击 [按钮] → 触发 [什么]
3. 系统展示 [内容] → 用户可以 [操作]
4. 完成 → 跳转到 [页面] 或展示 [反馈]

### 6.3 关键状态说明
- 空状态：首次使用时展示什么
- 加载状态：数据加载中的反馈
- 错误状态：异常情况的处理
- 成功状态：操作完成后的引导

## 7. 评估指标（200-300 字）

### 7.1 北极星指标
- 定义 1 个最能衡量产品价值的核心指标
- 说明为什么选这个指标

### 7.2 关键业务指标
| 指标 | 定义 | 目标值 | 统计周期 |
|------|------|--------|---------|
| 指标1 | 计算公式/口径 | 具体数字 | 日/周/月 |
| 指标2 | 计算公式/口径 | 具体数字 | 日/周/月 |
| 指标3 | 计算公式/口径 | 具体数字 | 日/周/月 |

### 7.3 数据埋点事件
列出需要埋点的 5-8 个关键事件：
| 事件名 | 触发时机 | 关键属性 |
|--------|---------|---------|
| page_view | 用户打开页面 | page_name, source |
| ... | ... | ... |

{ai_section}

---

## 生成质量检查清单（生成后自检）

在输出 PRD 前，确认以下所有条目都满足：
- [ ] 每个用户故事都有完整的 As a / I want / So that 格式
- [ ] 每个功能点都有至少 1 个 checkbox 验收标准
- [ ] 非功能需求全部包含量化数字（不是"快速"而是"≤ 2s"）
- [ ] 交互流程至少描述了 2 个完整流程
- [ ] 北极星指标只有 1 个，且有明确的定义
- [ ] 没有出现"优化体验""提升性能"等空洞表述
- [ ] 总字数 2500-4500 字

如果不满足任何一条，请修改后再输出。
"""
