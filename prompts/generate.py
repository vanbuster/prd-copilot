GENERATE_PROMPT = """基于以下信息，生成一份详尽、可直接交付给开发团队执行的 PRD（产品需求文档）。

## 用户原始需求
{user_input}

## 澄清问答
{clarify_qa}

## 是否为 AI 产品
{is_ai_product}

---

请严格按照以下模块结构生成 PRD。每个模块都有详细的输出要求，你必须逐条满足。
总字数要求 4000-6000 字，宁可多写不可遗漏。

## 1. 产品概述（400-600 字）

### 1.1 产品名称与定位
- 如果用户未提供产品名，根据功能起一个简洁的名称
- 用一句话定义产品定位："XX 是一款面向 [目标用户] 的 [产品类型]，核心解决 [什么问题]"

### 1.2 背景与问题
- 描述当前市场/用户面临的 **具体问题**（2-3 个痛点，每个痛点用 1-2 句话说明，并附带影响面：影响多少用户/多少收入）
- 说明为什么现在做这件事（市场时机、技术条件、用户需求变化等）
- 如有数据支撑（用户调研、市场趋势），在此引用

### 1.3 核心价值主张
- 用一句话概括核心价值
- 列出 3 个核心价值点，每个价值点包含：价值描述 + 对用户的意义 + 与竞品的差异

### 1.4 产品目标（SMART）
- 短期目标（3 个月内）：具体、可衡量、有截止时间
- 中期目标（6 个月内）：具体、可衡量、有截止时间
- 每个目标必须包含：指标名、当前值（如有）、目标值、达成时间

## 2. 目标用户（300-500 字）

### 2.1 用户画像
给出 2-3 个典型用户画像，每个画像包含：
| 字段 | 内容 |
|------|------|
| 姓名 | 虚构但真实的名字 |
| 年龄/职业 | 具体年龄段和职业 |
| 核心痛点 | 1-2 个最迫切的问题（必须是可感知的具体问题） |
| 使用动机 | 为什么会使用这个产品（不是"因为需要"，而是具体场景触发） |
| 使用频率预估 | 每天/每周/每月几次，基于什么判断 |
| 技术熟练度 | 新手/一般/熟练，影响什么设计决策 |
| 典型一天 | 简述该用户在什么时间、什么场景下会用到本产品 |

### 2.2 使用场景
- 列出 3-5 个典型使用场景，每个场景包含：**触发条件** + **用户行为** + **预期结果** + **成功标准**
- 标注主场景（P0）和次场景（P1）

### 2.3 用户旅程
选择 1 个核心场景，描述用户从"发现问题"到"问题解决"的完整旅程（5-8 步），每步标注情绪和痛点。

## 3. 用户故事与需求分析（500-800 字）

### 3.1 核心用户故事
列出 8-12 个用户故事，**每个故事必须编号**（US1, US2, ...），严格遵循以下格式：

**P0（必须有）：**
- **US1**: As a [具体角色], I want [具体操作/功能], So that [具体价值/目的]
  - 验收标准：
    - [ ] 正常路径：[具体可验证的条件]
    - [ ] 边界条件：[边界场景的处理]
    - [ ] 异常路径：[出错时的处理]

**P1（应该有）：**
- **US4**: （同上格式，包含验收标准）

**P2（可以有）：**
- **US8**: （同上格式，包含验收标准）

### 3.2 需求优先级矩阵
| 用户故事 | 用户价值 | 实现复杂度 | 优先级 | 依赖 |
|---------|---------|-----------|--------|------|
| US1 | 高/中/低 | 高/中/低 | P0 | 无 |
| US2 | 高/中/低 | 高/中/低 | P0 | US1 |

## 4. 功能需求（800-1200 字）

按功能模块分组（3-5 个模块），**每个功能必须编号**（F1, F2, ...）并关联到用户故事。

### 4.X [模块名称]

**模块目标**：一句话说明这个模块解决什么问题
**关联用户故事**：US1, US3

| 编号 | 功能点 | 触发条件 | 预期行为 | MoSCoW | 验收标准 |
|------|--------|---------|---------|--------|---------|
| F1 | 具体描述做什么 | 用户做了什么/系统发生了什么 | 系统如何响应，包含成功和失败的情况 | Must | - [ ] 标准1 - [ ] 标准2 |
| F2 | 具体描述做什么 | ... | ... | Should | - [ ] 标准1 |

**边界说明**：
- 这个模块包含：...
- 这个模块不包含：...
- 模块间依赖：本模块依赖 [模块Y] 的 [功能]

**业务规则**：
- 列出该模块涉及的业务逻辑规则（如：权限判断规则、状态流转规则、计算公式等）

## 5. 非功能需求（300-500 字）

**每个维度必须包含量化指标**，为技术选型提供硬约束。

### 5.1 性能要求
| 指标 | 目标值 | 测量条件 | 影响的技术决策 |
|------|--------|---------|--------------|
| 页面加载时间 | ≤ X 秒 | 首屏加载，3G网络 | 是否需要 SSR/CDN |
| API 响应时间 | ≤ X ms | 95% 请求，并发 X 用户 | 是否需要缓存层 |
| 并发支持 | X 用户同时在线 | 峰值时段 | 服务器架构 |
| 数据处理 | X 条记录/秒 | 批量操作场景 | 是否需要异步队列 |

### 5.2 可用性要求
| 指标 | 目标值 |
|------|--------|
| 系统可用性 | ≥ X%（即全年停机 ≤ X 分钟） |
| 数据备份频率 | 每 X 小时 |
| 故障恢复时间 | ≤ X 分钟（RTO） |
| 数据丢失容忍 | ≤ X 分钟数据（RPO） |

### 5.3 安全要求
- 列出 3-5 条具体安全要求（认证方式、数据加密标准、权限控制模型、合规标准等）
- 标注每条对应的 OWASP 分类

### 5.4 兼容性要求
- 支持的浏览器/设备/系统版本及最低版本号

### 5.5 可扩展性要求
- 预期用户增长量级（3 个月/6 个月/1 年）
- 数据增长量级及对存储的影响

## 6. 信息架构与交互流程（400-600 字）

### 6.1 页面/模块结构
用缩进列表展示层级结构：
```
产品首页
├── 模块A
│   ├── 子页面A1
│   └── 子页面A2
├── 模块B
│   ├── 子页面B1
│   └── 子页面B2
└── 设置中心
    ├── 个人设置
    └── 系统设置
```

### 6.2 核心交互流程
用编号步骤描述 2-3 个核心流程，**每个步骤必须包含异常分支**：
**流程1：[流程名称]**（关联 US1）
1. 用户进入 [页面] → 看到 [什么]
   - 异常：[如果 X 不满足，展示什么]
2. 用户点击 [按钮] → 触发 [什么]
   - 异常：[如果操作失败，如何处理]
3. 系统展示 [内容] → 用户可以 [操作]
4. 完成 → 跳转到 [页面] 或展示 [反馈]

### 6.3 关键状态说明
- 空状态：首次使用时展示什么（引导文案 + 操作提示）
- 加载状态：数据加载中的反馈（骨架屏/转圈 + 超时处理）
- 错误状态：异常情况的处理（错误码 + 用户可执行的操作）
- 成功状态：操作完成后的引导（下一步建议 + 数据变化提示）

## 7. 评估指标（300-400 字）

### 7.1 北极星指标
- 定义 **1 个**最能衡量产品价值的核心指标
- 说明为什么选这个指标（而非其他候选指标）
- 目标值和统计周期

### 7.2 关键业务指标
| 指标 | 定义（计算公式/口径） | 当前基线 | 目标值 | 统计周期 | 负责团队 |
|------|----------------------|---------|--------|---------|---------|
| 指标1 | ... | ... | ... | 日/周/月 | ... |
| 指标2 | ... | ... | ... | 日/周/月 | ... |
| 指标3 | ... | ... | ... | 日/周/月 | ... |

### 7.3 数据埋点事件
列出需要埋点的 5-8 个关键事件：
| 事件名 | 触发时机 | 关键属性 | 对应指标 |
|--------|---------|---------|---------|
| page_view | 用户打开页面 | page_name, source, referrer | DAU |
| ... | ... | ... | ... |

## 8. 不做什么（200-300 字）

**这是一个必须的模块，不是可选的。**

| 不做的功能/需求 | 排除原因 | 未来是否考虑 |
|---------------|---------|------------|
| 功能 X | MVP 阶段优先级不足，用户调研显示需求度低 | V2 可能加入 |
| 功能 Y | 技术实现成本过高，ROI 不合理 | 暂不考虑 |
| 功能 Z | 与核心定位不符 | 不做 |

## 9. 假设与约束（200-300 字）

### 9.1 核心假设
| 假设 | 验证方式 | 不成立时的备选方案 |
|------|---------|-----------------|
| 用户愿意在移动端完成此操作 | 用户调研/AB测试 | 优先做桌面端 |
| 现有支付系统能支撑新功能 | 技术预研 | 引入新支付渠道 |

### 9.2 约束条件
- 时间约束：交付截止时间
- 技术约束：必须使用的技术栈/不能使用的技术
- 资源约束：团队规模、可用人力
- 合规约束：需要满足的法规/标准

## 10. 风险评估（200-300 字）

| 风险 | 影响 | 概率 | 缓解措施 | 负责人 |
|------|------|------|---------|--------|
| 第三方 API 不稳定 | 核心功能不可用 | 中 | 设计降级方案 + 本地缓存 | 后端负责人 |
| 用户量超预期 | 性能下降 | 低 | 提前做压测 + 弹性扩容方案 | 运维负责人 |
| 合规要求变更 | 需要返工 | 低 | 持续关注法规动态 | 产品经理 |

## 11. 里程碑与发布计划（200-300 字）

| 阶段 | 时间 | 核心功能范围 | 交付标准 | 依赖 |
|------|------|------------|---------|------|
| MVP | X月X日 | US1-US4（核心流程闭环） | 核心流程可用 + 无 P0 bug | 设计稿完成 |
| V1.0 | X月X日 | US1-US8（完整功能） | 验收标准全通过 + 性能达标 | MVP 反馈收集 |
| V1.5 | X月X日 | US9-US12（增强功能） | 新功能上线 + 旧功能优化 | V1.0 稳定运行 |

### 发布标准
- 功能：所有 P0 用户故事验收通过
- 性能：非功能需求指标达标
- 质量：无 P0/P1 bug，P2 bug ≤ X 个
- 埋点：核心埋点事件已部署
- 文档：用户帮助文档已更新

{ai_section}

---

## 生成质量检查清单（生成后逐项自检，不满足则修改后重新输出）

- [ ] 每个用户故事都有编号（US1, US2...）和完整的 As a / I want / So that 格式
- [ ] 每个用户故事都包含正常路径 + 边界条件 + 异常路径的验收标准
- [ ] 每个功能点都有编号（F1, F2...）、触发条件、预期行为
- [ ] 非功能需求全部包含量化数字（不是"快速"而是"≤ 2s"）且标注了影响的技术决策
- [ ] 交互流程至少描述了 2 个完整流程，每个步骤都有异常分支
- [ ] "不做什么"模块至少列出 3 项
- [ ] 假设与约束模块至少列出 3 个假设
- [ ] 风险评估至少列出 3 个风险且每个都有缓解措施
- [ ] 里程碑至少分 2 个阶段，每个阶段有明确交付标准
- [ ] 北极星指标只有 1 个，且有明确的定义和目标值
- [ ] 没有出现"优化体验""提升性能""简单易用"等空洞表述
- [ ] 所有业务规则和计算逻辑都有明确描述
- [ ] 总字数 4000-6000 字

如果不满足任何一条，请修改后再输出。永远不要在 PRD 中使用空洞的描述。
"""
